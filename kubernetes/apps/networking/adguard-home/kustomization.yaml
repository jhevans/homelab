apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - repository.yaml
  - release.yaml
  - ingress.yaml

configMapGenerator:
  - name: adguard-home-config
    files:
      - AdGuardHome.yaml
