apiVersion: apps/v1
kind: Deployment
metadata:
  name: paperless-redis
  namespace: productivity
spec:
  selector:
    matchLabels:
      app: paperless-redis
  template:
    metadata:
      labels:
        app: paperless-redis
    spec:
      containers:
      - name: redis
        image: docker.io/library/redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: paperless-redis
  namespace: productivity
spec:
  selector:
    app: paperless-redis
  ports:
  - port: 6379
    targetPort: 6379
