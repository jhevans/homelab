apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai
resources:
  - pvc.yaml
  - rbac.yaml
  - statefulset.yaml
  - service.yaml
  - secrets.yaml
  - ingress.yaml

configMapGenerator:
  - name: hermes-config
    files:
      - config.yaml
