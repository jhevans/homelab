apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: forgejo
  namespace: development
spec:
  interval: 1h
  chart:
    spec:
      chart: forgejo
      version: "16.2.0"
      sourceRef:
        kind: HelmRepository
        name: forgejo
        namespace: development
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    # Disable built-in subcharts
    postgresql:
      enabled: false
    postgresql-ha:
      enabled: false
    redis:
      enabled: false
    redis-cluster:
      enabled: false
    # Basic configuration for Forgejo
    gitea:
      config:
        APP_NAME: "Forgejo: Home Lab"
        RUN_MODE: prod
        server:
          DOMAIN: forgejo.lab.local
          ROOT_URL: "http://forgejo.lab.local/"
          SSH_PORT: 2222
        database:
          DB_TYPE: sqlite3
        metrics:
          ENABLED: true
    service:
      http:
        port: 3000
    persistence:
      enabled: true
      size: 5Gi
