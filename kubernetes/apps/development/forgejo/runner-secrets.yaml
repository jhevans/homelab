apiVersion: v1
kind: Secret
metadata:
  name: forgejo-runner-secret
  namespace: development
stringData:
  # The registration token from Forgejo UI: Admin Panel -> Actions -> Runners -> Registration Token
  token: "PLACEHOLDER_REGISTRATION_TOKEN"
